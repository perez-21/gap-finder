# Environment Configuration
NODE_ENV="development" # Options: 'development', 'production'
PORT="8080"            # The port your server will listen on
HOST="localhost"       # Hostname for the server

# CORS Settings
CORS_ORIGIN="http://localhost:8080" # Allowed CORS origin, adjust as necessary

# Rate Limiting
COMMON_RATE_LIMIT_WINDOW_MS="1000"  # Window size for rate limiting (ms)
COMMON_RATE_LIMIT_MAX_REQUESTS="20" # Max number of requests per window per IP

QUESTIONS_PER_PROBE="3"
GAP_THRESHOLD="0.75"
RECENCY_WEIGHT_MULTIPLIER="1.5"
AI_MODEL=""
AI_MAX_RETRIES="1"

# Environment Configuration
NODE_ENV=development # Options: 'development', 'production'
PORT=3000            # The port your server will listen on
HOST=localhost       # Hostname for the server

# Authentication
JWT_SECRET=supersecretstring
JWT_EXPIRES_IN=7200

# AI Providers
LOCAL_LLM_BASE_URL=http://127.0.0.1:1234
LOCAL_LLM_API_TOKEN=lm-studio

# Database

TEST_DATABASE_URL=postgresql://username:pass@localhost:5432/test_db_name
DATABASE_URL=postgresql://usernaame:pass@localhost:5432/db_name